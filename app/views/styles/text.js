import { Colors, Typography, Spacing } from '../../assets/styles';

export default title = {
    color: Colors.text,
    fontSize: Typography.textSize,
    textAlign: "center",
    margin: Spacing.textMargin
}